from django.shortcuts import render

# Create your views here.
from .models import ToDoList, Task

# def todo_list_view(request, todolist):
#     con = {
#         'title' : todolist.title,
#         'id' : todolist.id,
        
#     }
#     return render(request,"templates/todolist_view.html", con)

